# myAlphaView V2.7 第二阶段升级

本版以已正常运行的 V2.6 为基线，不改变 `CORE_TIERS`、20/30/50 资金分档、100 倍期权乘数、Short Ask / Long Bid 和 Alpaca 密钥兼容逻辑。

## 页面可见变化

- 期权持仓区新增“刷新全部持仓”按钮和刷新进度。
- 登录后自动检查缺失报价；美股常规交易时段每 15 分钟顺序检查。
- 页面进入后台后暂停自动请求，回到页面再检查。
- 休市时显示“休市 · 上次报价”，不再误报为接口过期。
- 休市及过期报价的盈亏显示为灰色“约”，明确只是参考。
- 行情覆盖区分“实时行情覆盖”和“休市参考覆盖”。
- 美股指数/VIX 常规时段每 90 秒检查，休市每 15 分钟检查。
- A股/港股交易窗口每 2 分钟检查，休市每 15 分钟检查。

## 稳定性变化

- Alpaca、Yahoo、MarketData 外部请求增加 10 秒超时。
- Alpaca 429 返回明确的可重试状态和 `Retry-After`。
- 美股行情上游短暂失败时返回当前 Edge Function 实例的上次成功缓存，并标注缓存状态。
- 多资产预算改为按资产分别防抖保存，避免快速编辑时前一个资产未保存。
- 退出登录后清除页面中的私人预算数值。
- GitHub Action 增加并发锁、推送前 rebase 和额外 JavaScript 检查。

## 部署

1. 将完整目录内容覆盖到 GitHub 仓库根目录。
2. 提交后，`Deploy Supabase Functions` 应因两个 Function 文件变化而自动运行。
3. 确认 `Deploy Supabase Functions` 和 `Daily Dashboard Update` 均为绿色。
4. 浏览器强制刷新，确认页面版本和期权模块均显示 V2.7。

本版没有新增数据库迁移；V2.6 的 `202609220001_stock_targets_private.sql` 仍只需执行一次。
