# myAlphaView V3.5.0 策略信号视觉重构

## 本次调整

- 取消核心ETF策略卡中的预留资金输入、20%/30%/50%金额分配与清除预算操作。
- 保留 QQQM/QQQ、VGT、QLD、TQQQ、VOO 原有差异化三级回撤阈值，不改变策略口径。
- 每张卡直接展示当前回撤、下一档触发价、距当前价格以及三级阈值对应的实际价格。
- 已触发状态改为“进入第N档加仓区”，只做纪律提醒，不自动下单。
- 策略引擎由深蓝黑背景改为浅暖灰卡片，降低视觉压迫；触发状态使用克制的琥珀色和绿色分级。
- 删除前端 `strategy-budget.js` 依赖。数据库中的旧预算记录不主动删除，以免造成不可逆的数据损失。

## 部署

1. 完整覆盖压缩包文件并提交到 GitHub。
2. 等待 GitHub Pages 自动部署，或手动运行 `Daily Dashboard Update`。
3. 浏览器强制刷新：Mac `Cmd+Shift+R`；Windows `Ctrl+F5`。

本次没有数据库迁移，也不需要重新部署 Supabase Functions。
