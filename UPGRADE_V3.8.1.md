# V3.8.1 更新说明：休市请求降频

这是 V3.8.0 的额度优化补丁，不改变 TQQQ X2、LEAPS、期权、账户或观察池数据结构。

## 浏览器刷新节奏

- 美股正常盘中：每 5 分钟刷新。
- 盘前和盘后：每 30 分钟检查。
- 工作日处于正常交易时间、但行情源确认并非 `REGULAR`：按节假日处理，每 2 小时检查。
- 普通周末：页面打开时检查一次，之后停止自动轮询。
- 标签页隐藏时继续停止刷新；重新显示时检查一次。

## Edge Function 缓存

- 正常盘中：1 分钟。
- 盘前和盘后：30 分钟。
- 工作日节假日：2 小时。
- 周末：6 小时。
- Yahoo 真实指数全部可用时，不再额外请求 MarketData 备用行情。

## 上传后必须执行

本补丁修改了 `supabase/functions/market-snapshot/index.ts`，因此覆盖代码后除了运行 `Daily Dashboard Update`，还必须运行仓库中的 Supabase 部署工作流，或单独部署 `market-snapshot` Edge Function。否则页面端会停止周末轮询，但服务端仍使用旧的 20 秒缓存。
